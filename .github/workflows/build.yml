name: PyNet CI
on: [push]
jobs:
  Build-And-Test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
                
      - name: Get CUDA 
        run: .github\workflows\install-cuda-windows.ps1
          
      - name: Running cmake
        run: |
          cd ${{ github.workspace }}
          mkdir build
          cd build
          cmake ../PyNet.Infrastructure 
      
      - name: Make Modules Public
        run: | 
          ../../.github/Add-BmiSetting "PyNet.Infrastructure.vcxproj"
          ../../.github/Add-BmiSetting "libs/PyNet.Models.Cuda/PyNet.Models.Cuda/vcxproj
        
                
      - name: Build Solution
        run: |
          cmake --build ./
          dir
